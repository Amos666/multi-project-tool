# 工作台功能整合需求文档 v2（精简版）

**—— DevWorkbench 原型 × Multi-Project Tool 现有扩展**

> 本文件为需求基线文档，后续需求变化统一在此文件上修改维护。
> 原型来源：`z:\workspace\multi-workspaces-tool\ui-prototype\index.html`

---

## 1. 整合原则

- **零侵入**：现有 5 个 Tab（Git / ProjectsCmd / ShortCutCmd / Python / Set）不动，新消息协议使用独立前缀
- **新 Tab 承载**：工作台功能全部通过新增 Tab 实现，沿用 `hiddenTabs` 显隐机制
- **排除项**：❌ Git 概览、❌ 番茄钟、❌ 开发者工具箱

## 2. 精简决策一览

| 原型功能 | 决策 | 理由 |
|---|---|---|
| Git 概览（分支/变更/PR） | **移除** | 与现有 Git Tab 职责重叠 |
| 节点类型 7 种 | **精简为 5 种**：命令、条件分支、并行(Fork)、汇合(Join)、通知 | 「触发器」无实际作用（点击执行即触发）；「文件操作」可由 Shell 命令替代 |
| 执行模式三选一 | **保留串行/并行**，移除「混合模式」 | 混合模式与流程图 Tab 完全重复 |
| 状态栏 JDK 切换 | **移除** | 用命令本身或现有 Shell 配置控制即可 |
| 模拟节点失败开关 | **移除** | 原型演示用途；失败路径可直接用真实命令验证 |
| 环境切换 dev/test/prod | **保留但简化** | 并入执行前的单一下拉，不做全局状态栏常驻 |
| 模板区 + 执行历史 | **从工作台移入「工作流 Tab」侧栏** | 与画布强相关，就近操作 |
| 快速启动器索引项 | **精简** | 只保留：执行/停止工作流、模板加载、批量任务组、Tab 跳转 |

精简后的 Tab 布局：

```
现有 Tab（不动）                    新增 Tab
┌─────┬──────────┬─────────┬────────┬─────┬────────┬──────────┬──────────┐
│ Git │ProjectsCmd│ShortCut │ Python │ Set │ 📋 清单 │ 🔀 工作流 │ ⚡ 批量执行 │
└─────┴──────────┴─────────┴────────┴─────┴────────┴──────────┴──────────┘
```

---

## 3. 功能需求

### 3.1 新 Tab A：今日清单（原"工作台"瘦身后）

| 编号 | 需求 |
|---|---|
| A-1 | 任务按 🔴紧急 / 🟡常规 / 🟢低优先级 三级分组展示 |
| A-2 | 添加任务（输入框 + 优先级 + 回车确认）；勾选完成（划线置灰）；悬停显示删除 |
| A-3 | 进度展示：完成百分比 + 进度条；「清除已完成」一键移除 |
| A-4 | 数据经扩展存储持久化，跨会话保留 |

### 3.2 新 Tab B：工作流（核心，含模板与历史）

**B-1 画布编排（左：节点面板｜中：SVG 画布｜右：属性面板）**

| 编号 | 需求 |
|---|---|
| B1-1 | 5 类节点：命令、条件分支、并行(Fork)、汇合(Join)、通知，色条区分 |
| B1-2 | 节点增/删/拖拽（边界限制 + 网格吸附）；删除节点级联删边 |
| B1-3 | 连线模式：起点高亮 → 点目标完成；拒绝重复连线、拒绝成环（DAG）；点连线删除 |
| B1-4 | 属性面板：名称、命令/说明、**超时时间（真实生效）**、失败策略（停止/跳过/重试1次）、条件节点的**判定表达式配置**、只读上游依赖 |
| B1-5 | 工作流保存/读取/清空，以 JSON 文件持久化；支持多份工作流切换 |
| B1-6 | 状态视觉：执行中蓝色脉冲 / 成功绿 / 失败红 / 跳过灰虚线；同层多节点自动绘制并行区域框 |

**B-2 执行引擎（扩展侧新增服务层）**

| 编号 | 需求 |
|---|---|
| B2-1 | DAG 拓扑调度，Fork/Join 真并行（子进程执行真实命令） |
| B2-2 | 失败策略与超时真实生效（超时 kill 子进程） |
| B2-3 | 命令支持现有全局参数 `${var}` 替换（只读复用 JSON Tab 体系） |
| B2-4 | 执行前选择：环境下拉（dev/test/prod，生产带警示）+ Shell 类型（复用现有多 Shell 能力） |
| B2-5 | 手动停止：终止子进程，未启动节点标记跳过 |
| B2-6 | 每节点日志独立追踪（对齐现有日志设计） |

**B-3 Tab 内布局扩展**

| 编号 | 需求 |
|---|---|
| B3-1 | **模板区**（画布左侧栏内）：内置 3 模板（Maven 全量含并行 / 多模块并行 / 测试→条件→部署）；保存当前为自定义模板，可回显/加载/删除 |
| B3-2 | **执行历史**（画布左侧栏内）：结果图标 + 工作流名 + 耗时 + 时间，置顶、持久化、点击查看该次执行明细 |
| B3-3 | **底部监控面板**：执行明细表（时间/节点/状态/耗时，行内「查看」定位节点）+ 日志输出区（分色、自动滚底、按节点过滤）+ 状态摘要（状态/计时/失败数/跳过数） |
| B3-4 | 执行互斥：执行中锁定画布编辑并给出视觉提示 |

### 3.3 新 Tab C：批量执行

| 编号 | 需求 |
|---|---|
| C-1 | 线性流程特例，与 B-2 引擎共用 |
| C-2 | 任务组下拉（预置组可配置）；模式仅两选：串行 / 并行 |
| C-3 | 命令有序列表：拖拽排序，逐条状态 + 耗时 |
| C-4 | 一键生成流程图并跳转工作流 Tab |
| C-5 | 与 ProjectsCmd 并存不合并（后者保留多行脚本 + 共享变量场景） |

### 3.4 全局：快速启动器（Ctrl+K）

| 编号 | 需求 |
|---|---|
| D-1 | Webview 内浮层，模糊搜索 + ↑↓/Enter/Esc 导航 |
| D-2 | 索引项（精简后）：执行/停止工作流、模板加载、批量任务组、ShortCutCmd 快捷命令（只读引用）、Tab 跳转 |

### 3.5 设置与持久化（追加项）

| 编号 | 需求 |
|---|---|
| E-1 | `hiddenTabs` 覆盖 3 个新 Tab，Set Tab 仅增显隐开关 |
| E-2 | i18n 新增中/英词条 |
| E-3 | 新增独立存储域：清单、工作流、模板、历史；不动现有配置结构 |

---

## 4. 分期建议

| 期次 | 范围 |
|---|---|
| **一期（核心）** | 工作流 Tab：画布编排 + 执行引擎 + 监控面板 + 模板 + 历史 + 持久化 |
| **二期** | 批量执行 Tab、今日清单 Tab |
| **三期** | 快速启动器、画布缩放/自动布局等体验增强 |

## 5. 风险边界

- MainViewProvider 已近 4000 行 → 新 Tab 参照 `gitTabProvider` 拆分为独立文件
- 真实执行引擎是主要新增工作量：子进程管理、超时 kill、并行数上限
- 所有新增 command 使用 `workflow` / `checklist` / `batch` / `launcher` 前缀，与现有协议零冲突

---

## 变更记录

| 日期 | 变更内容 |
|---|---|
| 2026-08-21 | 初版 v2：基于原型整理需求；移除番茄钟、开发者工具箱、Git 概览；节点类型精简为 5 种；执行模式精简为串行/并行 |
